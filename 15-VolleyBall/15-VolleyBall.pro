TEMPLATE = app
CONFIG += console c++17
CONFIG -= app_bundle
CONFIG -= qt

SOURCES += \
        main.cpp

# CONFIG += link_pkgconfig
# PKGCONFIG += sfml-all

# INCLUDEPATH += /usr/include
# LIBS += /usr/lib/libbox2d.so
# LIBS += /usr/lib/libbox2d.so.3
# LIBS += /usr/lib/libbox2d.so.3.0.0
